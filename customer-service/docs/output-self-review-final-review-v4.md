# R7 最终放行终审（t50）— 判定：**pass**

> 评审者：reviewer（独立判定；只写本文件，未改动任何制品或既有报告）
> 日期：2026-09-11｜全部读数为**本次现采**
> 被审对象：`docs/output-self-review-delivery.md`（mtime `05:53:06`，43894 B，**sha16 `f3cd56647a77fa59`**）与 `docs/output-self-review-requirements.md`
> **复核时点纪律**：材料在本轮**被连续编辑三次**（`05:51:28` → `05:53:06` …）。我**每次都先等其稳定、再取判据**（末次连续 3 次 ×7 秒采样一致：mtime `05:53:06.002`、43894 B、`sha16 = f3cd56647a77fa59`）⇒ **本报告全部结论针对该已稳定版本**，不针对移动中的文档（本轮教训⑰「不在写入窗口内取读数」的自我适用）。
> 证据层级（G-ANCHOR）：本报告结论均来自本机实测（现采 sha256、逐字 grep 判读、团队状态现采比对、真实 `FenceMachine` 直驱），非转述；**captain 的自查读数一律不被采信，逐条独立复跑**。
> 判据纪律（已遵守）：**判据不嵌行号**（一律用符号名/节名）；**sha 现采**；**不用「多采样一致」单独作冻结证据**；**未扫描其他成员环境**。

---

## 0. 结论

**verdict = pass**。t47 的两条阻塞项**均已真闭环**，我独立复核 captain 的两处编辑**内容准确、无自我认证风险**；t43 的 8 项文档修复**未被本轮编辑破坏**；制品**零改动**成立。

**必须如实记录的一点（t50 acceptance 具名要求）**：本轮 `delivery.md` 的两处编辑**由 captain 亲自完成**（非 engineer-core）——captain 既做裁定又做编辑，属**自我认证风险**场景。**我依要求做了独立复核且未采信其自查**：其 7 项自查读数中**多项在「全文 grep」口径下不可复现**（见 §3.2），但**逐项内容我均独立验证为真**（差异是**口径/时点**问题，非内容错误）。**该风险已被本次独立复核消解，不影响 pass**。

---

## 1. t50 契约 verify 命令（逐条实跑）

| # | 命令 | 结果 | 判定 |
|---|---|---|---|
| 1 | `grep -c '已知残留（captain 裁定，本轮不修）' delivery.md` | **4**（§6.7 说明行 1 + 三项数据行 3） | ✅ |
| 2 | `grep -c '待办 0 项' delivery.md` | **1** | ✅ |
| 3 | `grep -n 't44' delivery.md \| head` | 命中 §13 教训⑬（含 objective 等值比对证据）、§10.3 表、§10.3.1 —— 均在位 | ✅ |
| 4 | `grep -c '已失效基线' requirements.md` | **1** | ✅ |
| 5 | `sha256sum index.html fence-machine.ts engine.ts` | `6b4bc36bd549c020…` / `93b35f1b0eb9e127…` / `2da07bdce590e6ad…` | ✅ |

---

## 2. R6-1 / R6-2 闭环核验（逐条独立复核）

### 2.1 R6-1：三项代码类 finding 已改标「已知残留」并含影响面 + 成本评估 ✅

| 检查点 | 独立复核结果 |
|---|---|
| 状态词已改 | `R4-7`/`R4-8`/`R4-9` 三行**均为「◑ 已知残留（captain 裁定，本轮不修）」**；**「需另立任务」在三项行内为 0 命中**（不再混用）✅ |
| ① 影响面 | §6.7 三项行内**逐项含「影响面」**（R4-9 行以「该类回归只能靠人工补测发现」表述，且 §6.8 表内该行显式给出影响面）✅ |
| ② 将来修改成本 | 三项行内**逐项含「将来修改」**，且给出**改动文件 + 是否需重开冻结段**（F-6「只改测试、不涉制品，成本最低」／F-5「改 1 文件 1 分支，需重开 `plugins/**`」／F-4「改 2 文件跨两侧，成本最高」）✅ |
| 理由链四要素 | **非 blocker** ✅ ｜ **不影响核心保证**（输出前自审 + 不通过降级 + 绝不外发源码）✅ ｜ **重开冻结的代价与收益不成比例** ✅ ｜ **如实记录优于匆忙修补** ✅ |
| 汇总 | **「待办 0 项」** 在位，且 §6.7 汇总行明示「✅ 已处置 6 项 ｜ ◑ 已知残留 3 项 ｜ 待办 0 项」✅ |
| 放行判断 | 「本轮**不遗留待办**」段落在位 ✅ |

### 2.2 R6-2：§10.3/§10.3.1 已覆盖两变体并记录 objective 逐字相同 ✅

- **§10.3 标题**：已改为「**非终态「完成」的依赖不释放门禁（`cancelled` 与 `failed` 两种变体）**」✅；
- **实例表**：现含 **5 行实例**，覆盖两变体 —— `t5 failed ⇒ t6`（连带 `t7`）、`t8 failed ⇒ t9`（连带 `t10`）、`t39 cancelled ⇒ t40`、`t36 cancelled ⇒ t37/t16`、**`t44 failed ⇒ t45/t46`**（覆盖 t47 要求的第三次实例）✅；
- **新增实例的真实性我逐条现采核对**（板面状态）：`t5`/`t8`/`t44` 均 `failed`，其下游 `t6`/`t9`/`t45`/`t46` 均 `pending` 且 **deps 确实指向该 failed 任务** ⇒ **新增实例为真，非杜撰** ✅；
- **§10.3.1** 已记录 `t44.objective == t41.objective` 逐字相同 ✅。

### 2.3 R6-2 的「独立复核」项（t50 明令现采重比）✅

我从**团队状态现采**两条 objective 重新比对（**未采信材料陈述**）：

```
t41 objective : len=186  sha16=90a057d3e55f839c
t44 objective : len=186  sha16=90a057d3e55f839c
等值比对 ==  True
```

⇒ 材料 §13 记录的 `sha16=90a057d3e55f839c` / `len=186` **与我的现采逐一相符** ✅；该事实**可独立复跑**，符合 acceptance 的「须现采可比对」要求。

---

## 3. 自我认证风险评估（t50 acceptance 第八条：披露制作者并判断风险）

### 3.1 事实披露

**本轮 `delivery.md` 的两处编辑由 captain 亲自完成**，非 engineer-core。captain 既做裁定（三项为「已知残留、本轮不修」）又做编辑（改状态词、补影响面/成本、改 §10.3 标题与新实例）⇒ **属自我认证风险场景**。**已按要求在报告中如实记录**。

### 3.2 风险核查：captain 的自查读数**未被采信**，我逐条独立复跑

| captain 自查声明 | 我的全文 grep 现采 | 是否相符 | 说明 |
|---|---|---|---|
| `已知残留（captain 裁定，本轮不修）` → 3 | **4** | ⚠️ 口径差 | 差异 = §6.7 **说明行**也含该串（3 数据行 + 1 说明行）；captain 显然只数了数据行 |
| `影响面` → 3 ｜ `将来修改` → 3 | **7** ｜ **5** | ⚠️ 口径差 | 其 3 应为「§6.8 表内三项行」的口径；全文还含 §6.8 表头、§6.7 行内、§8 回滚表「影响面」列等 |
| `不成比例` → 1 ｜ `不影响核心保证` → 1 | **4** ｜ **4** | ⚠️ 口径差 | 该表述在 §6.7 汇总、§6.8 理由链、§6.8 两表结论中**各出现多次**（本次编辑后本就重复出现） |
| `逐字相同` → 2 | **3** | ⚠️ 口径差 | 第 3 处为 §13 教训⑬ 的第二个实证行 |
| `待办 0 项` → 1 | **1** | ✅ 相符 | — |
| `index.html = 6b4bc36bd549c020`（未变） | `6b4bc36bd549c020` | ✅ 相符 | — |

**判定**：差异**全部是「计数口径未声明」**（captain 数的是「表内数据行」，我数的是「全文匹配行」），**不是内容不实**。其自称的**四要素与三行状态，我逐项按内容验证均为真**（§2.1）。⇒ **不构成 finding**；但**恰好复现本轮已定稿的两条纪律**：
1. **判据须带 scope/计数口径**（G-ANCHOR-5、教训④「穷尽清单四件套」）——同一个字符串在「表内 / 全文 / 关键字次数」三种口径下分别为 `3 / 4 / 9`，**不带口径的计数不可复核**；
2. **「不自采信自查」是必要的**：若非我独立复跑，这 5 处口径差会被当作「数字对不上」而误判为编辑错误；反之若我盲信自查，则会漏掉口径问题。**两条路都走错，故必须独立复算**。

### 3.3 结论

**自我认证风险已被本次独立复核消解**：编辑**内容**全部为真（§2.1–§2.3 逐项验证），**唯一瑕疵是自查读数的口径未声明**（属方法问题、非事实问题）。**建议（不阻塞放行）**：captain 若在交付说明中引用这些计数，须**补 scope 口径**或直接改引「数据行 = 3」这类带限定语的表述。

---

## 4. t43 的 8 项文档修复**未被本轮编辑破坏** ✅（t50 acceptance 第六条）

| 项 | 我本轮现采复核 | 判定 |
|---|---|---|
| **R4-1** §16.1 | `需要求文档` 仍含「**⚠️ 已失效基线（时点 `2026-09-10T20:21:40+08:00`，v3.2.11）**」；`grep -c '已失效基线'` = **1** | ✅ 未被破坏 |
| **R4-2** D2 | 期望仍为**相对表述「≥1」**（G-ANCHOR-5）在位 | ✅ |
| **R4-3** D4 | 「差 **3** 而非差 1」的口径更正仍在位 | ✅ |
| **R4-4** 两侧同口径 | `两侧同口径` 命中 **2**；**「仅主机侧」零违规**——全文唯一命中为**禁止句**「不得只归主机侧」（我逐字核对其上下文确认是禁令，非断言） | ✅ |
| **R4-5** F-2 | `模型重写\|二次校验\|定向修复\|REP1` = **5**（仍 ≥1，§6.6① 在位） | ✅ |
| **R4-6** SRC4 | `要求文档` 内「**判据范围限定**」在位 | ✅ |
| **R4-8** passed≠已验证 | 「**不等于**『真机已验证』」面向读者声明在位 | ✅ |
| **R4-9** P9 | 「**四情形枚举**」+ **G-ANCHOR-6** 均在位 | ✅ |

---

## 5. 制品零改动（现采证据）

```
web/guest/index.html                     6b4bc36bd549c020  141804 B  mtime 04:49:29
plugins/output-gate/src/fence-machine.ts 93b35f1b0eb9e127  19283 B  mtime 02:07:03
plugins/output-gate/src/engine.ts        2da07bdce590e6ad  20028 B  mtime（前一日）21:48:21
```

- 三项与 t50 acceptance 给出的参考值**逐一相符**；
- **mtime 全部早于本轮编辑（材料 `05:51:28`）与本报告写作时刻** ⇒ 本轮为**纯文档**改动，**未触碰制品**；
- **判定：✅ 成立**。附注：此处未以「多采样一致」单独作冻结证据，而是**现采值 == acceptance 参考值 == 材料 §2 指纹表**三者互证 + mtime 早于本轮编辑。

---

## 6. R6-3（我 t47 报的重复表问题）—— **实质已消解，仅余非阻塞冗余**

### 6.1 我 t47 报的**互斥**已被 captain 修掉 ✅

t47 时 §6.8 内两张表对 F-5 给出**互斥**结论（表 1「核心保证在该分支**无**兜底」 vs 表 2「**不影响**核心保证」）。**现采**：

```
「核心保证在该分支无兜底」 = 0   （旧互斥句已删）
「核心保证仍有兜底」       = 1   （表 1 已改为与表 2 同向的表述）
⇒ 两表对 F-5 的结论方向现已一致（均为「有兜底 / 不影响核心保证」）
```

**⇒ R6-3 的「互斥」实质已消除**，captain 在未声明的编辑中**顺手修掉了它**（其消息未提及此项，属**未被声明的修复**，我在此记录）。

### 6.2 我独立验证「哪一表正确」——用实测

把「门禁缺失 ⇒ 前端 `fence-fallback` 为唯一防线」作为条件，对**硬编码冻结副本**跑 5 类输入（段落·行首闭合／段落·行首未闭合／表格·同格／表格·跨行／普通代码块内嵌受控围栏），每例**先自检「输入确含块体」**（防空断言）：

```
泄漏用例 = 0/5
⇒ 前端兜底路径确实守住「绝不外发源码」⇒ 现两表所持的「有兜底 / 不影响核心保证」**与实测一致**
```

### 6.3 剩余冗余（**非阻塞，建议下轮清理**）

§6.8 内仍**并存两张**「三项残留 × 影响面/成本」表（`| # | 残留 |` 表头 **2** 个，「三项共同结论」**2** 条）。二者内容**不再互斥**，但**高度重复**（三项残留 × 同维度 × 同结论）。
**性质**：属**文档冗余**（同一事实两处表述），**不构成事实错误、不影响读者判断**（两表结论同向）。**故不阻塞本次放行**；若后续有文档整理窗口，建议**删其一并保留单条「三项共同结论」**（以含「路径 B / 路径 B 流式期 / ③.9c 三入口 / ③-b·③-c 共 88 项全绿」证据链的那张为准）。

---

## 7. 流程缺陷实例收录（t50 acceptance 相关的四项）

| # | 要求 | 现采状态 | 判定 |
|---|---|---|---|
| ① | `cancelled`/`failed` 不释放依赖门禁 | §10.3 **含 5 个实例**覆盖两变体，含 `t44⇒t45/t46`；并给出「直接阻塞 = 6 个任务 / 5 个阻塞源」的汇总 | ✅ 已收录（较 t47 更完整） |
| ② | objective 抄被取代任务 | §10.3.1 + §13 教训⑬「第二个实证」**双处收录**，含 `sha16=90a057d3e55f839c` / `len=186` / `== True` 的可复核证据 | ✅ 已收录 |
| ③ | 写入型反证误写仓内真实制品路径（两次） | 教训⑲：明写「**出现两次**」，含危害类别（污染并发取证方观测） | ✅ 已收录 |
| ④ | captain 指控前未验证主体（误指 verifier） | 教训⑲：明写「captain 一度**误指 verifier**，已撤回并记录**新一类流程错误：『指控前未验证主体』**」 | ✅ 已收录 |

---

## 8. 结论与放行

**verdict = pass**

**依据（逐条对齐 t50 acceptance）**：
1. **R6-1 闭环** ✅ —— 三项已改标「已知残留（captain 裁定，本轮不修）」，**「需另立任务」不再混用**，每项含 **① 影响面 ② 将来修改成本评估**（含改动文件与是否需重开冻结段），理由链四要素齐备；
2. **R6-1 语义核验** ✅ —— §6.7 汇总明示「**待办 0 项**」，并有「本轮不遗留待办」的放行判断段；
3. **R6-2 闭环** ✅ —— §10.3 标题与内容覆盖 `cancelled` **与** `failed`，含 `t39⇒t40`、`t36⇒t37/t16`、**`t44⇒t45/t46`**（我另核 `t5⇒t6`、`t8⇒t9` 两个新增实例为真）；§10.3.1 记录 objective 逐字相同；
4. **R6-2 独立复核** ✅ —— 我**现采**两条 objective 重比，`sha16` 双双 `90a057d3e55f839c`、`len=186`、`== True`，与材料记录相符；
5. **制品零改动** ✅ —— 三项指纹现采与 acceptance 参考值逐一相符，mtime 均早于本轮编辑；
6. **t43 的 8 项修复未被破坏** ✅ —— §4 逐项复核均在位；
7. **报告落 `output-self-review-final-review-v4.md`** ✅ —— 未改任何制品或既有报告；
8. **披露制作者 + 自我认证风险判断** ✅ —— 已如实记录「由 captain 亲自编辑」，并**未采信其自查**（§3.2 逐条独立复跑）：其 7 项自查中 5 项为**口径差**（非内容错误），4 项内容要素逐项验证为真 ⇒ **风险已消解**。

**非阻塞建议（不影响放行，供后续文档整理）**：
1. §6.8 两张重复表建议**删一留一**（结论已同向，纯冗余）；
2. 若在交付说明中引用判别器/计数，**须补 scope 与计数口径**（同一串在「表内/全文/关键字次数」下为 `3 / 4 / 9`，不带口径不可复核）。

**复跑入口（均可独立复现）**：
```
grep -c '已知残留（captain 裁定，本轮不修）' customer-service/docs/output-self-review-delivery.md
grep -c '待办 0 项' customer-service/docs/output-self-review-delivery.md
grep -c '已失效基线' customer-service/docs/output-self-review-requirements.md
sha256sum customer-service/web/guest/index.html customer-service/plugins/output-gate/src/{fence-machine,engine}.ts
node /tmp/rev-r7-final.mjs     # 三项状态词 / 理由链四要素 / R6-3 互斥是否消解 / 双表冗余
node /tmp/rev-f5-backstop.mjs  # 门禁缺失 ⇒ 前端兜底 5 形态零泄漏（判定哪一表正确）
```

---

## 9. 本稿的自证与局限（G-ANCHOR）

- **审阅对象**：仅两份文档 + 制品**只读取指纹**；**未修改任何制品或既有报告**（inScope 仅本文件）；
- **副作用**：本轮**未跑任何测试套件**（避免重写 `tests/gate-dom-snapshots.json`），全部为**只读命令**与**内存内 `FenceMachine` 直驱**（脚本无写盘操作）⇒ **零制品副作用**；
- **未验证（不默认通过）**：线上 10800 的门禁真实生效（容器为旧镜像）、`serviceVersion` 目标值、真机几何判据自然样本——与材料 §5 一致，**均未默认通过**；
- **未做的事（按 captain 指示）**：未扫描/轮询任何其他成员的环境、端口或进程；**历史积压消息一律丢弃**，本轮只处理 t50 契约与其点名的复核项。
